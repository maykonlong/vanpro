# Riscos aceitos

Cada item aqui é um problema **conhecido, avaliado e deliberadamente não
corrigido agora**. A lista existe porque risco silencioso é pior que risco
assumido: se não estiver escrito, na próxima auditoria alguém vai descobrir de
novo, do zero, e não vai saber se foi decisão ou esquecimento.

Todo item precisa de: o que é, por que não foi corrigido, o que acontece se
explorado, e **o que destrava a correção**. Item sem data de revisão não é risco
aceito — é risco esquecido.

---

## R-001 · `deepmerge-ts` fixado por `override` até a migração para o Prisma 7

**O que é.** `@prisma/config@6.19.3` fixa `deepmerge-ts@7.1.5`, que tem
esgotamento de pilha ao mesclar grafos recursivos (advisory `high`). A correção
aplicada é um `override` em `api/package.json` levando a dependência para
`^8.0.2` — a versão corrigida, com a mesma API que o `@prisma/config` usa.

**Por que isso não virou "risco aceito de desenvolvimento".** A primeira leitura
foi que `prisma` era só `devDependency` e portanto não ia para a imagem. Estava
errado: na linha 6, `prisma` é dependência de **runtime** do `@prisma/client`
(`npm ls prisma --omit=dev` confirma), e ela está mesmo dentro do contêiner —
66,9 MB em `node_modules/prisma`. A vulnerabilidade estava em produção, e
aceitá-la teria sido aceitar o que nem foi medido direito.

**O que resta.** O `override` é uma trava, não uma solução: ela vale enquanto o
`@prisma/config` não subir sozinho a dependência. A saída definitiva é migrar
para `prisma@7`, que exige `prisma.config.ts` e muda o caminho de saída do
generator — tentado durante o hardening, quebra a geração do cliente, e migração
de major merece PR próprio com os 269 testes como rede.

**Como verificar que continua valendo.**
`cd api && npm ls deepmerge-ts` precisa mostrar `8.x`, e
`npm audit --omit=dev --audit-level=high` precisa sair 0 (é o gate do CI).

**Revisar até:** 2026-12-09.

---

## R-002 · `PARENT` não tem tela própria de faturas

**O que é.** O responsável vê as cobranças do filho pela exportação LGPD
(`GET /privacy/export`), e não por uma listagem dedicada: `/financial/invoices`
é restrita à gestão.

**Por que não foi corrigido.** Expor a rota financeira ao responsável exigiria
um recorte novo (só as faturas dos próprios filhos), e recorte de rota
financeira é onde vazamento entre empresas nasce. Preferiu-se a rota que já
nasce escopada por `parentId` no servidor.

**Exposição real.** Nenhuma de segurança; é limitação de produto. O responsável
consegue o dado, com menos conforto.

**O que destrava.** `GET /financial/invoices/minhas`, escopada por `parentId`,
com teste de acesso cruzado antes do merge.

**Revisar até:** 2026-12-09.

---

## R-003 · Cobertura baixa em módulos que dependem de rede externa

**O que é.** `asaas.service.ts` 11%, `whatsapp.service.ts` 0%,
`realtime/server.ts` 0%, `uploads` 30%, `ai` 37%.

**Por que não foi corrigido.** A regra do projeto é testar contra o sistema de
verdade, não contra mock. Esses arquivos falam com Asaas, Meta e WebSocket —
cobri-los com `vi.mock` produziria número alto e garantia nenhuma, que é
exatamente o que as 71 fixtures da versão anterior faziam enquanto o sistema
vazava dados.

**Exposição real.** O caminho *desligado* está coberto (sem credencial →
`503 FEATURE_DISABLED`, verificado). O caminho *ligado* nunca foi exercitado
contra o gateway. Ele é **NÃO VERIFICADO** — e não "provavelmente funciona".

**O que destrava.** Sandbox do Asaas com credencial de teste no CI, e um teste de
contrato com a Meta. Antes de ligar cobrança real em produção, isso deixa de ser
risco aceito e passa a ser bloqueio.

**Revisar até:** 2026-12-10 — e, de todo modo, antes do primeiro cliente
pagante, o que vier primeiro. A data existe porque "antes do primeiro cliente"
não é verificável por script nenhum: some da conta e ninguém volta a olhar.

---

## R-004 · Vínculo de dispositivo na sessão é heurística, não prova

**O que é.** O refresh confere o hash do `User-Agent` de origem da sessão.

**Por que não foi corrigido.** Não existe correção: o `User-Agent` é enviado
pelo cliente e quem rouba o cookie copia o cabeçalho junto. Não é uma falha de
implementação, é o limite da técnica.

**Exposição real.** Eleva o custo do replay cego e denuncia o caso comum de
cookie exportado, mas **não** impede um atacante atento. A defesa que de fato
segura é a rotação com detecção de reuso, que já está implementada e testada.

**O que destrava.** Nada previsto. Registrado para que ninguém leia o código e
conclua que o vínculo de dispositivo é garantia — o comentário em
`session.service.ts` diz o mesmo.

**Revisar até:** 2027-03-10. `n/a` fazia o item desaparecer da contagem — e
limitação documentada continua sendo limitação: vale reler daqui a seis meses
para conferir se ainda é a melhor que dá para fazer.

---

## R-005 · Triagem dos achados do blindar 0.83.2 (rodada de 2026-09-10)

**O que é.** A rodada completa saiu com `26 crit / 48 high` e exit 2 (NO-GO).
Cada um foi conferido na fonte, um por um. Nenhum sobreviveu à conferência —
e essa frase só vale porque abaixo está *onde* olhar em cada caso.

Isto não é dispensa: é registro. Achado sem `fp` no aceite conta como **novo**
na próxima rodada, que é exatamente o comportamento desejado.

### Segredos e certificados apontados em arquivos que não estão no repositório

`gitleaks` (16), `trivy` (3) e parte do `semgrep` apontaram `.env`, `api/.env`,
`api/.env.test`, `infra/certs/*.key`.

Conferido: `git check-ignore -v` confirma os cinco caminhos ignorados, e
`git ls-files` lista apenas `.env.example` e `api/.env.example`. **Nenhum deles
está versionado.** São material gerado na máquina por
`infra/scripts/gen-secrets.mjs` e `infra/scripts/gerar-certificados.sh` — o
scanner varre a árvore de trabalho, não o histórico.

O que restaria de risco real seria o vazamento pelo disco da máquina, que é
problema de host (escopo do `ancorar`), não do repositório.

`fp`: gitleaks `fp:30316e10 fp:4ed79f39 fp:8a401e55 fp:8e64541d fp:a230291f fp:fd3b5155`,
trivy `fp:01a79602 fp:c037377a fp:fae28e7a`.

### O SHA que fixa a action virou "API key" por entropia

`gitleaks` marcou `.github/workflows/ci.yml:53` — que é
`actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020`.

Fixar action por SHA é a defesa recomendada contra comprometimento de tag; o
detector de entropia não distingue um SHA de 40 caracteres de uma credencial.
Remover a fixação para calar o scanner seria trocar segurança por silêncio.

`fp`: `fp:fe22410e`.

### O `semgrep` apontou artefatos de cobertura e o hash-isca

`api/coverage/**` é saída de build (ignorada pelo git). **Removida do disco**
nesta rodada — artefato que ninguém lê não deve existir para ser varrido.

`api/src/modules/auth/password.service.ts:22` é o `DUMMY_HASH`, constante
deliberada usada por `dummyCompare()` para gastar o mesmo tempo quando o e-mail
não existe. Um hash bcrypt não é credencial: ele não abre nada. Removê-lo
reintroduziria o oráculo de tempo que revela quais e-mails existem.

`fp`: `fp:052862b5 fp:05e0bbf2 fp:143a3dc0 fp:194ff54a fp:219cf4dc fp:21a0ba3e fp:5033adc7 fp:5b0b87a9
fp:6b405c4f fp:7fa5f114 fp:8b68d3b7 fp:b411dd26 fp:b7eceddf fp:ca8d8c4f fp:ff2f80d4`.

### Os comentários que descrevem o defeito foram lidos como o defeito

Três achados, todos da mesma natureza — e são os mais divertidos da rodada:

- `check-cryptography` acusou "uso de MD5". A única ocorrência no projeto é
  `infra/postgres/pg_hba.conf:23`, num comentário que explica **por que** o
  banco usa `scram-sha-256` **e não** md5.
- `check-mock-killer` acusou "botão sem handler real" em
  `web/e2e/helpers.ts:433`, num comentário que explica que um teste ingênuo
  aprovaria um `onClick={() => {}}`.
- `check-fintech-banking-br` acusou "handler financeiro sem audit log" em
  `webhooks.controller.ts`, que tem três chamadas a `audit()` (linhas 74, 108
  e 167).

`fp`: `fp:643f6d3a fp:a49d9867 fp:719f9994` + os 25 do mock-killer listados abaixo.

### `console.*` em ferramenta de infraestrutura

25 achados do `mock-killer` em `infra/scripts/*.mjs` — que são scripts de
linha de comando cuja saída **é** o produto. `console.log` num script de guarda
não é log vazando para produção; é o resultado da guarda.

O guarda próprio do projeto (`guards.sh`, item "console.* no codigo") olha
`api/src` e `web/src`, e passa limpo.

`fp`: `fp:03b96c15 fp:162c8e38 fp:29c81b89 fp:441a5478 fp:5d2546fd fp:6188ec2c fp:78ad81cd fp:83c86323
fp:856a82f6 fp:8d7d03ee fp:a9e18cd0 fp:b13641d6 fp:b346012b fp:b5680ac3 fp:c45b8671 fp:d2775754
fp:d4ea0415 fp:d51b3a98 fp:e5ae751e fp:ea6e32a0 fp:efa54d8a fp:f1202055 fp:f1d96840 fp:f585a77d
fp:f6e5644b`.

### CSP com `unsafe-inline`

`check-network-security` apontou `api/src/http/app.ts`. As duas ocorrências
estão dentro de `if (env.APP_ENV !== 'production')`, num
`helmet.contentSecurityPolicy` montado **apenas** em `/api/v1/docs`, para o
Swagger UI. A CSP global, linhas 50 e 55, é `["'self'"]` nos dois casos.

`fp`: `fp:1d597e4f fp:a0b33fd3`.

### Heurísticas que não enxergaram a defesa

- **"56 endpoints sem guard"** (sem `file`, sem `line`). Contraprova:
  `node infra/scripts/audit-route-guards.mjs` → *69 rotas com
  requireRole/requireSuperAdmin, 8 em controller declarado público, **0 sem
  guarda***. A fronteira `app.use(authenticate)` e o `requireRole(...GESTAO)`
  com spread não são visíveis ao grep do checker.
- **"0 endpoints LGPD"** e **"sem política de privacidade"**. Há 8 rotas em
  `api/src/modules/privacy/privacy.controller.ts` e a página
  `web/src/pages/public/PrivacyPolicy.tsx`, cujo conteúdo é verificado pelo
  E2E. Não há analytics no produto, logo não há cookie de analytics a
  consentir.
- **"entrypoint não faz `exec "$@"`"**. `infra/postgres/entrypoint-tls.sh`
  termina com `exec docker-entrypoint.sh "$@"` — honra o CMD; o checker
  procurava a forma literal.
- **"7 actions usando tag em vez de SHA"**.
  `grep -rn "uses:" .github/workflows/*.yml | grep -v "@[0-9a-f]\{40\}"` não
  devolve nada: **todas** estão fixadas por SHA.
- **"log despeja o objeto inteiro da requisição"** em
  `api/src/security/rate-limit.ts`. As quatro chamadas a `logger` passam
  `{ err, prefix, path }` e a chave do limite; nenhuma passa `req`.
- **`wave-guardian` sem `run-report.json`**: ele roda antes do report existir.
  É ordenação do orquestrador, não achado do projeto.

`fp`: `fp:bff04693 fp:31e79005 fp:33b15fdb fp:d9e5fd8c fp:ba795416 fp:4daca03f fp:6707dcf3
fp:8fd15bd8`.

### Portas publicadas de `postgres` e `redis`

`check-api-surface-isolation` está certo quanto ao princípio: banco e cache não
devem aceitar chamada externa. Aqui elas são publicadas em
`127.0.0.1:5433` e `127.0.0.1:6380` — **loopback**, não `0.0.0.0`, com o
comentário no `docker-compose.yml:40` explicando exatamente isso. A porta existe
para a suíte de testes e o `psql` da máquina alcançarem o banco.

Em servidor, a exposição de host é decidida pelo firewall e é escopo do
`ancorar` — que **ainda não foi rodado contra host nenhum**, e por isso
`DEPLOYMENT` continua com host **NÃO VERIFICADO**.

`fp`: `fp:65987884`.

**Revisar até:** 2026-12-10 — ou antes, na próxima rodada do blindar: um `fp`
que reaparecer sobre arquivo que mudou não é mais o mesmo achado, e volta a ser
novo.

---

## R-006 · O que a rodada do blindar deixou aberto, e por quê

Da segunda passagem dos 53 playbooks sobraram achados `high` que **não** foram
corrigidos. Cada um abaixo é uma decisão, não um esquecimento — e nenhum deles é
"defesa ausente": são limites de escala, de escopo de produto ou de custo de
migração.

Treze outros achados `high` da mesma leva **foram corrigidos** e continuam
aparecendo no relatório porque foram medidos sobre um commit anterior: segundo
fator por bandeira, imagem sem a correção no ar, ROPA/DPIA ausentes, E2E fora do
CI, log sem rotação, convite que não chegava, imagens por tag mutável, e a trava
da suíte que abortava. Estão fechados em `da819b0`, `0330e89` e `5e9f2e4`.

### A borda de saída do produto não existe neste ambiente

**O que é.** Não há provedor de e-mail, o gateway de cobrança está desligado e o
canal de WhatsApp não tem sequer campo de telefone no schema. Convite, redefinição
de senha, cobrança e publicação **não saem** do sistema.

**Por que não foi corrigido.** Ligar qualquer um deles exige credencial de um
terceiro e uma decisão comercial (qual provedor, qual conta, quem paga). O que
estava ao alcance foi feito: nenhuma dessas telas **mente** mais. O convite
devolve o link para quem o emitiu copiar e entregar; a recuperação de senha diz
que esta versão não envia e-mail e indica o caminho real; a cobrança e a
publicação recusam com `503` em vez de carimbar sucesso.

**Exposição real.** Zero risco de segurança. É funcionalidade ausente, declarada.

**O que destrava.** Uma conta de provedor transacional (e as cláusulas de
transferência internacional, se for fora do Brasil — ver `docs/LGPD-ROPA-E-DPIA.md`).

**Revisar até:** 2026-12-10. `fp:` sem impressão — achado gravado por playbook,
que não computa fingerprint.

### Data e hora sem fuso (`TIMESTAMP` em vez de `TIMESTAMPTZ`)

**O que é.** 57 colunas de data/hora em 23 tabelas guardam horário sem fuso.

**Por que não foi corrigido.** É migração destrutiva sobre todas as tabelas do
produto, com reescrita de dado e janela de indisponibilidade — exatamente o tipo
de mudança que este projeto proíbe fazer no meio de um ciclo de hardening.

**Exposição real.** Hoje, baixa: aplicação e banco rodam em `America/Sao_Paulo`,
e o produto é de uma cidade só. Ela aparece quando houver frota em outro fuso, ou
no dia do horário de verão se ele voltar — e aí aparece como ponto batido na hora
errada, que é registro de jornada.

**O que destrava.** Primeiro cliente fora do fuso de São Paulo, ou o retorno do
horário de verão. Migração com `USING ... AT TIME ZONE 'America/Sao_Paulo'`, em
janela declarada, com `ACEITE-DESTRUTIVO.md` ao lado.

**Revisar até:** 2027-03-10.

### Tempo real e uploads não escalam para além de uma réplica

**O que é.** `socket.io` sem adaptador de Redis: com duas réplicas, quem está na
réplica A não recebe a posição publicada na B. E o upload grava em disco local
(`fs.writeFile`), então a foto enviada numa réplica não existe na outra.

**Por que não foi corrigido.** Os dois são a mesma decisão: o produto roda hoje
em uma réplica, e resolver de verdade significa adaptador de Redis mais
armazenamento de objetos — dependência nova e serviço novo, cada um com o próprio
custo operacional, para um problema que ainda não existe.

**Exposição real.** Nenhuma enquanto houver uma réplica. **Total** no minuto em
que alguém subir a segunda: o mapa da van some para metade dos responsáveis e
metade das fotos "desaparece", de forma intermitente e difícil de diagnosticar.

**O que destrava.** A decisão de escalar horizontalmente. É pré-requisito, não
melhoria: `@socket.io/redis-adapter` (o Redis já está lá) e S3/compatível para os
uploads, **antes** da segunda réplica — não depois.

**Revisar até:** 2027-03-10, ou no dia em que a segunda réplica for cogitada.

### Latência de login e saturação sob carga

**O que é.** `POST /auth/login` leva ~940 ms em média (bcrypt custo 12 no event
loop). `GET /students` vai de 503 ms com 1 conexão a 3 s de p95 com 20
simultâneas, numa base de 96 alunos.

**Por que não foi corrigido.** O custo 12 do bcrypt é deliberado e documentado: é
a faixa que torna a força bruta cara. Baixá-lo para melhorar um número de latência
seria trocar segurança por benchmark. A saturação da listagem tem causa conhecida
(o mesmo processo faz hash de senha e serve consulta) e a solução — mover o hash
para fora do event loop, ou separar processos — é mudança estrutural.

**Exposição real.** Uma frota escolar tem dezenas de logins por dia, não milhares.
O número incomoda; hoje não machuca ninguém.

**O que destrava.** Medição em cliente real que mostre fila de verdade. Aí a saída
é `worker_threads` para o bcrypt, não custo menor.

**Revisar até:** 2027-03-10.

### Sem disjuntor em chamada a terceiro

**O que é.** Asaas e WhatsApp têm teto de tempo por requisição, mas não têm
circuit breaker: com o terceiro fora, cada chamada continua pagando o timeout.

**Por que não foi corrigido.** As duas integrações estão **desligadas**. Um
disjuntor sobre código que nunca executa é complexidade que ninguém exercita — e
este projeto já pagou caro por defesa não exercitada.

**Exposição real.** Zero hoje. Vira alta no mesmo dia em que a cobrança for
ligada.

**O que destrava.** Ligar a cobrança. O disjuntor entra junto, no mesmo PR, com
experimento de caos que o exercite — não depois.

**Revisar até:** antes de ligar a cobrança, e no máximo 2026-12-10.

### Prática de caos não existe como rotina

**O que é.** Os dois únicos experimentos de caos já executados neste projeto
foram os desta rodada (congelar o Redis, antes e depois da correção). Não há
cadência nem runbook.

**Por que não foi corrigido.** Um experimento de caos vale pela hipótese que
refuta. Os dois que rodaram refutaram uma hipótese escrita no próprio código — e
foi assim que o defeito mais grave da rodada apareceu. Institucionalizar isso é
trabalho de operação contínua, não de um ciclo de hardening.

**Exposição real.** Média: modos de falha não exercitados continuam sendo
descobertos em produção.

**O que destrava.** Um experimento por trimestre, começando pelo que já se sabe
frágil: banco lento (não fora), disco cheio, e relógio fora de sincronia — este
último agora relevante por causa do TOTP.

**Revisar até:** 2026-12-10.

